
// A $( document ).ready() block. to start 
$( document ).ready(function() {
    alert( "Crystal Collector" + "   \n ready!" );
    var firstGuess = $("#guess-first"); 

var GuessValue = 0; 

//generating random number for beginning of game 
    //click event function
  
    //how to call the function if not calling it in html doc inline 
 
//getting the value typed in ""firstGuess" id="guess-first" 
//


crystalValue.parseInt(); 
});
function getRandomNumber () {
    var randomNum = Math.random() * 10; //setting randomNum to a Math.random
    
    $("#math-output").text  = "Your Random Number is" + randomNum; 
    $("#math-output").text  = "Your Random Number is" + randomNum;
}
function randomNumberGen () {
    var randomNumbre = Math.random() *10; 
    $("#computer-guess")
}
function getGuessValue() {
    //trying to append that value when it is clicked in our html 
    firstGuess.val().trim() 
    //appending first guress value to computer guess 
    $("computer-guess").text = firstGuess + guessValue ; 
}
